/* admin.wxss */
.admin-page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--color-bg-admin);
}

/* 顶部 */
.top-bar {
  padding: 28rpx 28rpx 20rpx;
  padding-top: calc(20rpx + env(safe-area-inset-top));
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.top-title { display: flex; flex-direction: column; }
.brand { font-size: 36rpx; font-weight: 700; letter-spacing: 1rpx; }
.sub { font-size: 22rpx; opacity: 0.85; margin-top: 4rpx; }

.icon-btn {
  width: 64rpx; height: 64rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.22);
  display: flex; align-items: center; justify-content: center;
}

/* 概览卡 */
.stats-scroll {
  background: linear-gradient(180deg, var(--color-primary-light) 0%, var(--color-primary-light) 50%, var(--color-bg-admin) 50%, var(--color-bg-admin) 100%);
  padding: 4rpx 0 12rpx;
}

.stats-row {
  display: inline-flex;
  padding: 8rpx 24rpx 12rpx;
  gap: 16rpx;
  white-space: nowrap;
}

.stat-card {
  display: inline-flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: center;
  width: 160rpx;
  padding: 20rpx 22rpx;
  background: #fff;
  border-radius: 20rpx;
  box-shadow: var(--shadow-md);
  flex-shrink: 0;
}

.stat-num {
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.1;
  color: var(--color-text);
}

.stat-label {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  margin-top: 6rpx;
}

.stat-card.total .stat-num { color: var(--color-primary); }
.stat-card.pending .stat-num { color: var(--color-warning); }
.stat-card.urgent .stat-num { color: var(--color-danger); }
.stat-card.overdue .stat-num { color: var(--color-danger); }
.stat-card.completed .stat-num { color: var(--color-success); }
.stat-card.archived .stat-num { color: var(--color-text-tertiary); }
.stat-card.processing .stat-num { color: var(--color-info); }

/* 工具栏 */
.toolbar {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx 12rpx;
  gap: 8rpx;
}

.search-wrap { flex: 1; min-width: 0; }

.tool-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 10rpx 12rpx;
  font-size: 22rpx;
  color: var(--color-text-secondary);
  background: #fff;
  border-radius: 16rpx;
  min-width: 80rpx;
  box-shadow: var(--shadow-sm);
}

.tool-btn text { margin-top: 2rpx; }

.tool-btn.tool-active {
  background: var(--color-primary-bg);
  color: var(--color-primary);
}

/* 阶段 tab */
.tabs-scroll {
  padding: 8rpx 0 16rpx;
}

.tabs-row {
  display: inline-flex;
  padding: 0 24rpx;
  gap: 12rpx;
  white-space: nowrap;
}

.tab-chip {
  padding: 10rpx 24rpx;
  background: #fff;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  border-radius: 999rpx;
  flex-shrink: 0;
  box-shadow: var(--shadow-sm);
}

.tab-chip-active {
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  box-shadow: 0 4rpx 12rpx rgba(255, 136, 0, 0.3);
}

/* 列表头信息 */
.list-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8rpx 28rpx 12rpx;
}

.list-total {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
}

.list-select-all {
  font-size: 24rpx;
  color: var(--color-primary);
  font-weight: 500;
}

/* 订单列表 */
.order-list {
  flex: 1;
  padding: 0 24rpx 160rpx;
  box-sizing: border-box;
}

.loading-block,
.loading-more {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx 0;
  color: var(--color-text-tertiary);
  font-size: 24rpx;
  gap: 12rpx;
}

.no-more {
  text-align: center;
  padding: 24rpx 0;
  color: var(--color-text-tertiary);
  font-size: 22rpx;
}

.empty-block { padding: 120rpx 0; }

.order-card {
  position: relative;
  display: flex;
  background: #fff;
  border-radius: 20rpx;
  margin-bottom: 16rpx;
  padding: 24rpx 24rpx 24rpx 32rpx;
  box-shadow: var(--shadow-sm);
  overflow: hidden;
  transition: all 0.2s ease;
}

.order-card:active { transform: scale(0.99); }

.card-selected {
  background: var(--color-primary-bg);
  box-shadow: 0 0 0 2rpx var(--color-primary), var(--shadow-md);
}

.card-stripe {
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 8rpx;
}

.stripe-normal { background: var(--color-divider); }
.stripe-urgent { background: var(--color-danger); }
.stripe-pending { background: var(--color-warning); }
.stripe-completed { background: var(--color-success); }

.card-urgent {
  background: linear-gradient(90deg, #fff1f0 0%, #fff 30%);
}

.card-check {
  display: flex;
  align-items: center;
  padding-right: 20rpx;
}

.check-box {
  width: 40rpx; height: 40rpx;
  border-radius: 50%;
  border: 2rpx solid var(--color-divider);
  background: #fff;
  display: flex; align-items: center; justify-content: center;
}

.check-box-on {
  background: var(--color-primary);
  border-color: var(--color-primary);
}

.card-body { flex: 1; min-width: 0; }

.card-row-1 {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
}

.role-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--color-text);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 320rpx;
}

.pill-group {
  display: flex;
  align-items: center;
  gap: 6rpx;
  flex-shrink: 0;
}

.status-pill {
  font-size: 22rpx;
  padding: 4rpx 14rpx;
  border-radius: 999rpx;
  font-weight: 500;
  white-space: nowrap;
}

.pill-normal { background: var(--color-primary-bg); color: var(--color-primary); }
.pill-urgent { background: #fff1f0; color: var(--color-danger); }
.pill-pending { background: #fffbe6; color: var(--color-warning); }
.pill-completed { background: #f0faf3; color: var(--color-success); }

.card-row-2 {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-bottom: 12rpx;
}

.customer { font-weight: 500; }
.dot-sep { margin: 0 10rpx; color: var(--color-text-tertiary); }
.tb-id { color: var(--color-text-tertiary); font-size: 22rpx; }

.card-row-3 { margin-bottom: 10rpx; }

.progress-mini {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.progress-mini-bar {
  flex: 1;
  height: 8rpx;
  background: var(--color-divider);
  border-radius: 999rpx;
  overflow: hidden;
}

.progress-mini-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--color-primary), var(--color-accent));
  border-radius: 999rpx;
}

.progress-mini-num {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  font-weight: 600;
  min-width: 60rpx;
  text-align: right;
}

.card-row-4 {
  display: flex;
  justify-content: space-between;
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}

/* 批量操作栏 */
.batch-bar {
  position: fixed;
  left: 0; right: 0; bottom: 0;
  background: #fff;
  box-shadow: 0 -6rpx 24rpx rgba(0, 0, 0, 0.1);
  padding-bottom: env(safe-area-inset-bottom);
  z-index: 99;
}

.batch-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 28rpx 8rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  font-weight: 600;
}

.batch-clear {
  color: var(--color-primary);
  font-weight: 500;
}

.batch-actions { padding-bottom: 16rpx; }

.batch-row {
  display: inline-flex;
  padding: 0 20rpx;
  gap: 12rpx;
  white-space: nowrap;
}

.batch-btn {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: var(--color-bg);
  border-radius: 16rpx;
  padding: 14rpx 22rpx;
  font-size: 22rpx;
  color: var(--color-text-secondary);
  min-width: 120rpx;
  flex-shrink: 0;
}

.batch-btn text { margin-top: 6rpx; }
.batch-btn:active { background: var(--color-divider); }

.batch-btn-primary { background: #f0faf2; color: var(--color-success); }
.batch-btn-primary:active { background: #d9f5dd; }
.batch-btn-danger { background: #fff1f0; color: var(--color-danger); }
.batch-btn-danger:active { background: #ffd6d3; }

/* 弹窗面板 */
.popup-panel {
  background: #fff;
  border-radius: 28rpx 28rpx 0 0;
  padding: 24rpx 0 calc(24rpx + env(safe-area-inset-bottom));
  max-height: 70vh;
}

.popup-title {
  font-size: 30rpx;
  font-weight: 600;
  padding: 8rpx 32rpx 20rpx;
  color: var(--color-text);
}

.popup-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 32rpx;
  font-size: 28rpx;
  color: var(--color-text);
}

.popup-row:active { background: var(--color-bg); }

.popup-row-active {
  color: var(--color-primary);
  font-weight: 600;
}

.popup-row-meta {
  color: var(--color-text-tertiary);
  font-size: 24rpx;
}

/* 筛选 */
.filter-panel {
  width: 600rpx;
  height: 100vh;
  background: #fff;
  display: flex;
  flex-direction: column;
}

.filter-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 32rpx 20rpx;
  padding-top: calc(32rpx + env(safe-area-inset-top));
  border-bottom: 1rpx solid var(--color-divider);
}

.filter-title { font-size: 32rpx; font-weight: 700; }

.filter-body {
  flex: 1;
  padding: 24rpx 32rpx;
  overflow-y: auto;
}

.filter-group { margin-bottom: 28rpx; }

.filter-label {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 12rpx;
}

.filter-picker {
  padding: 20rpx 24rpx;
  background: var(--color-bg);
  border-radius: 16rpx;
  font-size: 26rpx;
  color: var(--color-text);
}

.filter-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: 28rpx;
  padding: 20rpx 0;
}

.filter-footer {
  display: flex;
  gap: 16rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--color-divider);
}

.filter-footer t-button { flex: 1; }

/* 操作抽屉 */
.action-sheet {
  background: #fff;
  border-radius: 28rpx 28rpx 0 0;
  padding-top: 24rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
}

.action-row {
  display: flex;
  align-items: center;
  padding: 28rpx 32rpx;
  font-size: 30rpx;
  color: var(--color-text);
  border-bottom: 1rpx solid var(--color-divider);
}

.action-row text { margin-left: 20rpx; }
.action-row:active { background: var(--color-bg); }

.action-cancel {
  margin: 16rpx 32rpx 0;
  text-align: center;
  padding: 24rpx 0;
  background: var(--color-bg);
  border-radius: 16rpx;
  font-size: 28rpx;
  color: var(--color-text-secondary);
}

/* 新增订单表单 */
.form-popup {
  background: #fff;
  border-radius: 28rpx 28rpx 0 0;
  max-height: 88vh;
  display: flex;
  flex-direction: column;
}

.form-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 32rpx 20rpx;
  border-bottom: 1rpx solid var(--color-divider);
}

.form-title { font-size: 32rpx; font-weight: 700; }

.form-body {
  padding: 24rpx 32rpx;
  max-height: 70vh;
}

/* 分区 */
.form-section {
  margin-bottom: 28rpx;
  padding-bottom: 12rpx;
  border-bottom: 1rpx solid var(--color-divider);
}
.form-section:last-child { border-bottom: none; }

.form-section-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--color-primary);
  margin-bottom: 20rpx;
  padding-left: 16rpx;
  border-left: 6rpx solid var(--color-primary);
  line-height: 1.2;
}

.form-row-2 {
  display: flex;
  gap: 20rpx;
}
.form-col { flex: 1; }

.form-label-inline {
  font-size: 28rpx;
  color: var(--color-text);
}

.form-textarea {
  width: 100%;
  min-height: 140rpx;
  background: var(--color-bg);
  border-radius: 12rpx;
  padding: 16rpx 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

/* 多图网格 */
.img-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}
.img-item {
  position: relative;
  width: 180rpx;
  height: 180rpx;
  border-radius: 12rpx;
  overflow: hidden;
  flex-shrink: 0;
}
.img-thumb { width: 100%; height: 100%; display: block; }
.img-remove {
  position: absolute;
  top: 4rpx;
  right: 4rpx;
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.55);
  display: flex;
  align-items: center;
  justify-content: center;
}
.img-add {
  width: 180rpx;
  height: 180rpx;
  border: 2rpx dashed var(--color-divider);
  border-radius: 12rpx;
  background: var(--color-bg);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-text-tertiary);
  flex-shrink: 0;
}

.upload-icon { font-size: 56rpx; line-height: 1; color: var(--color-text-tertiary); }

.form-item { margin-bottom: 24rpx; }

.form-row-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.form-label {
  display: block;
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 12rpx;
}

.required { color: var(--color-danger); }

.form-input {
  width: 100%;
  height: 80rpx;
  background: var(--color-bg);
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-picker {
  height: 80rpx;
  line-height: 80rpx;
  background: var(--color-bg);
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: var(--color-text);
}

.form-footer {
  display: flex;
  gap: 16rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--color-divider);
}

.form-footer t-button { flex: 1; }

/* 危险操作区 */
.danger-zone {
  margin-top: 40rpx;
  padding: 24rpx;
  border: 1rpx dashed #f5cccc;
  border-radius: 16rpx;
  background: #fdf5f5;
}
.danger-title {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--color-danger);
  margin-bottom: 12rpx;
  letter-spacing: 1rpx;
}
.danger-desc {
  font-size: 22rpx;
  color: #a06060;
  line-height: 1.5;
  margin-bottom: 12rpx;
  letter-spacing: 1rpx;
}
.danger-desc--all {
  margin-top: 16rpx;
  color: var(--color-danger);
  font-weight: 500;
}
.danger-btn {
  text-align: center;
  padding: 16rpx 0;
  border-radius: 12rpx;
  font-size: 26rpx;
  letter-spacing: 2rpx;
}
.danger-btn--mock {
  background: #fff;
  color: var(--color-danger);
  border: 1rpx solid #f5cccc;
}
.danger-btn--all {
  background: var(--color-danger);
  color: #fff;
}
.danger-btn:active { opacity: 0.85; }
