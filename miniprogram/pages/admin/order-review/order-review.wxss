/* order-review.wxss - 订单审核优化版 */
.review-page {
  min-height: 100vh;
  background: var(--color-bg-admin);
  display: flex;
  flex-direction: column;
}

/* 顶部标题 */
.top-bar {
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
  padding: 32rpx 32rpx 36rpx;
  padding-top: calc(32rpx + env(safe-area-inset-top));
  color: #fff;
}
.top-title { display: flex; flex-direction: column; gap: 6rpx; }
.brand { font-size: 38rpx; font-weight: 600; letter-spacing: 1rpx; }
.sub { font-size: 24rpx; opacity: 0.88; }

/* Tab Chips */
.tabs-scroll {
  background: #fff;
  border-bottom: 1rpx solid var(--color-divider);
  white-space: nowrap;
}
.tabs-row {
  display: inline-flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx 24rpx;
}
.tab-chip {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  padding: 12rpx 24rpx;
  background: var(--color-bg);
  color: var(--color-text-secondary);
  border-radius: 999rpx;
  font-size: 26rpx;
  white-space: nowrap;
  transition: all 0.2s ease;
}
.tab-chip-active {
  background: var(--color-primary);
  color: #fff;
  box-shadow: 0 4rpx 12rpx rgba(255, 140, 58, 0.28);
}
.tab-chip-warn:not(.tab-chip-active) {
  background: var(--color-primary-bg);
  color: var(--color-deco-warm);
}
.tab-count {
  font-size: 22rpx;
  background: rgba(0,0,0,0.08);
  padding: 2rpx 12rpx;
  border-radius: 999rpx;
  min-width: 32rpx;
  text-align: center;
}
.tab-chip-active .tab-count { background: rgba(255,255,255,0.28); }

/* 搜索 */
.search-wrap {
  padding: 16rpx 24rpx;
  background: #fff;
  border-bottom: 1rpx solid var(--color-divider);
}

/* 列表 */
.order-list {
  flex: 1;
  padding: 16rpx 24rpx 32rpx;
  box-sizing: border-box;
}

/* 卡片 */
.order-card {
  position: relative;
  display: flex;
  background: #fff;
  border-radius: 18rpx;
  margin-bottom: 20rpx;
  overflow: hidden;
  box-shadow: var(--shadow-sm);
}
.order-card.card-urgent { box-shadow: 0 2rpx 14rpx rgba(224, 116, 88, 0.18); }

.card-stripe {
  width: 6rpx;
  background: var(--color-divider);
}
.stripe-pending  { background: var(--color-warning); }
.stripe-urgent   { background: var(--color-danger); }
.stripe-rejected { background: var(--color-danger); }
.stripe-normal   { background: var(--color-success); }

.card-body {
  flex: 1;
  padding: 22rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  min-width: 0;
}

.card-row-1 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}
.tb-id-wrap {
  display: flex;
  flex-direction: column;
  min-width: 0;
  flex: 1;
}
.tb-id-label {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  line-height: 1;
}
.tb-id {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-top: 6rpx;
  word-break: break-all;
}

.status-pill {
  padding: 6rpx 16rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  white-space: nowrap;
  flex-shrink: 0;
}
.pill-group {
  display: flex;
  align-items: center;
  gap: 6rpx;
  flex-shrink: 0;
}
.pill-pending  { background: var(--color-primary-bg); color: var(--color-deco-warm); }
.pill-urgent   { background: #fff1f0; color: var(--color-danger); }
.pill-rejected { background: var(--color-bg); color: var(--color-text-tertiary);  }
.pill-normal   { background: #f0faf2; color: var(--color-success); }

.card-row-2 {
  display: flex;
  align-items: center;
  gap: 10rpx;
  font-size: 26rpx;
  color: var(--color-text);
}
.role-name { font-weight: 500; color: var(--color-text); }
.dot-sep { color: var(--color-divider); }
.ip-name { color: var(--color-text-secondary); }

.card-row-3 {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
}
.meta-text { font-size: 24rpx; color: var(--color-text-secondary); }
.meta-text-light { color: var(--color-text-tertiary); }

.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
}
.mini-tag {
  padding: 4rpx 14rpx;
  border-radius: 6rpx;
  font-size: 22rpx;
  background: var(--color-bg);
  color: var(--color-text-secondary);
}
.mini-tag-danger { background: #fff1f0; color: var(--color-danger); }

.card-row-4 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 4rpx;
  padding-top: 12rpx;
  border-top: 1rpx dashed var(--color-divider);
}
.card-actions {
  display: flex;
  gap: 14rpx;
}
.btn-line {
  padding: 8rpx 22rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  border: 1rpx solid var(--color-divider);
  color: var(--color-text-secondary);
  line-height: 1.4;
}
.btn-reject {
  border-color: #ffd1d1;
  color: var(--color-danger);
  background: #fff;
}
.btn-approve {
  border-color: var(--color-primary);
  color: #fff;
  background: var(--color-primary);
}

/* 加载状态 */
.loading-more, .no-more {
  text-align: center;
  padding: 24rpx;
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10rpx;
}
.empty-block {
  padding: 120rpx 0;
  display: flex;
  justify-content: center;
}

/* ===== 详情弹窗 ===== */
.detail-popup {
  background: var(--color-bg-admin);
  border-radius: 24rpx 24rpx 0 0;
  width: 100%;
  height: 90vh;
  display: flex;
  flex-direction: column;
}
.detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx;
  background: #fff;
  border-radius: 24rpx 24rpx 0 0;
  border-bottom: 1rpx solid var(--color-divider);
}
.detail-title-wrap {
  display: flex;
  align-items: center;
  gap: 16rpx;
}
.detail-title { font-size: 32rpx; font-weight: 600; color: var(--color-text); }
.detail-status-pill {
  padding: 6rpx 16rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
}

.detail-content {
  flex: 1;
  padding: 0 24rpx;
  overflow-y: auto;
}

.review-banner {
  margin-top: 20rpx;
  padding: 20rpx 24rpx;
  background: var(--color-primary-bg);
  border-left: 6rpx solid var(--color-warning);
  border-radius: 12rpx;
}
.review-banner-title {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 26rpx;
  font-weight: 500;
  color: var(--color-text);
  margin-bottom: 8rpx;
}
.review-banner-text {
  display: block;
  font-size: 26rpx;
  color: var(--color-text-secondary);
  line-height: 1.6;
}

.detail-section {
  margin-top: 20rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
}
.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 16rpx;
  padding-left: 14rpx;
  border-left: 6rpx solid var(--color-primary);
  line-height: 1;
}
.detail-row {
  display: flex;
  padding: 12rpx 0;
  border-bottom: 1rpx solid var(--color-divider);
}
.detail-row:last-child { border-bottom: none; }
.detail-row .label {
  font-size: 26rpx;
  color: var(--color-text-tertiary);
  width: 180rpx;
  flex-shrink: 0;
}
.detail-row .value {
  font-size: 26rpx;
  color: var(--color-text);
  flex: 1;
  word-break: break-all;
}
.detail-row .value-strong { color: var(--color-primary); font-weight: 600; }

/* 身材数据 */
.body-data-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}
.body-item {
  background: var(--color-bg);
  border-radius: 12rpx;
  padding: 20rpx;
  text-align: center;
}
.body-label {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  display: block;
  margin-bottom: 8rpx;
}
.body-value {
  font-size: 30rpx;
  color: var(--color-text);
  font-weight: 600;
}
.body-unit {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  margin-left: 4rpx;
  font-weight: normal;
}

/* 选项 */
.options-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 14rpx;
}
.option-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 12rpx 22rpx;
  background: var(--color-bg);
  border-radius: 999rpx;
  font-size: 24rpx;
  color: var(--color-text-tertiary);
}
.option-item.active {
  background: var(--color-primary-bg);
  color: var(--color-primary);
}

/* 图片 */
.image-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14rpx;
}
.grid-img {
  width: 100%;
  aspect-ratio: 1 / 1;
  border-radius: 12rpx;
  background: var(--color-bg);
}

/* 备注 */
.remark-box {
  background: var(--color-bg);
  border-radius: 12rpx;
  padding: 20rpx;
}
.remark-text {
  font-size: 26rpx;
  color: var(--color-text-secondary);
  line-height: 1.7;
}

.footer-spacer { height: 32rpx; }

/* 底部操作 */
.detail-footer {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 32rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  border-top: 1rpx solid var(--color-divider);
}
.footer-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  padding: 22rpx 0;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 500;
}
.footer-btn-reject {
  background: #fff;
  color: var(--color-danger);
  border: 1rpx solid #ffd1d1;
}
.footer-btn-approve {
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  box-shadow: 0 6rpx 16rpx rgba(255, 140, 58, 0.3);
}
